# TokUp CLI

One-command tool to switch all your AI tools to TokUp API.

```bash
# Install
npm install -g tokup-cli

# Configure
tokup init YOUR_API_KEY

# Auto-detect and switch
tokup switch

# Current status
tokup status
```

Supports: OpenAI SDK, Claude Code, Cursor, Codex, LangChain.
