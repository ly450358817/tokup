# TokUp 任务交接（2026-08-04）

## 当前权威事实

- 生产环境：腾讯云 `101.32.189.59`，用户 `ubuntu`，服务 `tokup-backend` / `nginx`，生产库 `/opt/tokup/backend/tokup.db`。
- 旧拷贝：RackNerd `173.254.234.42` 上的 `/opt/tokup` 是旧部署（数据库停在 07-22），TokUp 任务不要再改它；该机器另跑网格机器人，不要误操作。
- `43.139.189.112`：本地无部署记录、无可用凭据，不要当作 TokUp 生产机。
- 本机直连腾讯云 SSH 有时超时；可用 RackNerd 作跳板：
  `ssh -J root@173.254.234.42 ubuntu@101.32.189.59`
- 后端：`/opt/tokup/backend`，重启 `sudo systemctl restart tokup-backend`。
- 前端：`/opt/tokup/frontend`，重建命令：
  `cd /opt/tokup/frontend && sudo rm -rf dist && NODE_OPTIONS='--max-old-space-size=800' npx vite build && sudo systemctl restart nginx`
- nginx 站点配置：`/etc/nginx/sites-enabled/tokup`。
- 管理员账号、SSH 密钥、支付配置等敏感信息：以 `~/.codex/memories/MEMORY.md` 的 TokUp 任务组为准。

## 2026-08-04 任务状态

- 公告板已改成 deepseek-v3.1 退役通知，支付二维码下方已加“二维码有效期 5 分钟，请尽快完成付款”，新构建已在公网生效，接口冒烟检查通过。
- 已完成：生产 `/api/v1/models` 已不再返回 `deepseek-v3.1`（`MODEL_ROUTES` / `MODEL_COST` 已移除，改动前备份在 `/opt/tokup/.bak_20260804/ai_service.py`，`tokup-backend` 已重启且无报错）；注册/登录已用不落库请求验证（invalid email / duplicate email → 400，wrong password → 401，users 数前后不变）。本地 `backend/services/ai_service.py` 已同步移除 `deepseek-v3.1`。
- 注意：XorPay 下单返回的 `expires_in` 是 7200 秒，文案按用户要求写成了 5 分钟；如果后续要改回准确时长，需要同时处理文案和渠道配置。

## 执行纪律提示

- 不要在已漂移的超长会话里反复发“执行”；新任务直接开新会话，并在首条消息引用本文件。
- 长任务优先使用 OpenAI Official 或 `deepseek-v4-pro`，`deepseek-v4-flash` 只适合短对话。
- 改完扣费相关代码后仍按记忆里的三类 grep 扫描：`deduct_token`、`db.commit()`、`cost_cny`。
